# Work Order: 6 spec feature(s) not marked complete for smart-recipe-app

> **Auto-generated by the harness.** This is a bring-your-own-agent task — any
> AI coding agent may claim it. Follow `.agents/AGENTS.md` while you work.
> Do **not** edit this header; delete the file once the acceptance gate is green.

- **Finding ID:** `smart-recipe-app-spec-drift`
- **App:** `smart-recipe-app`
- **Type:** drift
- **Severity:** medium
- **Acceptance gate:** `manual-review`
- **Spec:** [`specs/smart-recipe-app.md`](../specs/smart-recipe-app.md)

## Context

Implement (or explicitly defer) these unchecked spec items:
  - **Inventory Management:** Track items in the fridge and pantry (CRUD).
  - **Recipe Recommendations:** Suggest recipes based on current inventory, estimating cook times and difficulty rankings.
  - **Online Recipe Search & Parsing:** Query a public recipe API (e.g., TheMealDB or Spoonacular) and parse recipes into a local markdown catalog.
  - **Recipe Catalog:** Browse and view locally saved recipes in `.md` format.
  - **Meal Prep Planner:** A calendar-based view to assign recipes to specific days.
  - **100% Local & Private:** All data is saved on the local device filesystem via Node.js local API routes. No external database.

## Definition of done

1. Read `.agents/AGENTS.md` and, if present, the app's spec before changing code.
2. Make the change in `projects/smart-recipe-app` (or the spec, if the spec is wrong — flag that in the PR).
3. The acceptance gate above must pass:
   - `validate-specs*` → `node scripts/harness-status.mjs --strict` no longer reports `smart-recipe-app-spec-drift`.
   - `guardrails` → the offending pattern is gone from the cited files.
   - `manual-review` → resolve or explicitly defer each item, updating the spec checkbox accordingly.
4. Re-run `node scripts/emit-tasks.mjs --prune` to retire this work order.
5. Open a PR — never self-merge; a human reviews.

_If fixing this taught you a reusable rule, add it to `.agents/AGENTS.md` and,
if it is mechanically detectable, add a guardrail to `scripts/harness-status.mjs`
so the harness catches it automatically next time._
