version: 2
updates:
  # No root `directory: "/"` npm entry, deliberately. For an npm workspace,
  # Dependabot's root directive auto-expands to every workspace member —
  # which duplicates the six per-app entries below rather than adding
  # coverage. Verified, not assumed: a prior root entry produced PRs like
  # "bump next from 16.3.0 to 16.3.1" touching elder-care-planner,
  # smart-recipe-app, and travel-packing-app's package.json in one PR, while
  # each of those apps' own per-app entry opened an *identical* PR for the
  # same file. Removing the root entry loses proposals only for the root
  # package.json's own narrow devDependency (onnxruntime-web) and the
  # `overrides` block (sharp, adm-zip, xcode>uuid) — those move rarely and
  # deliberately (the overrides exist for CVE patches, see PR #182/#278) and
  # are easy to track by hand; that's a small trade against eliminating
  # duplicate PRs across five dependency families and all six apps.
  - package-ecosystem: "npm"
    directory: "/projects/mood-diner"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      typescript-eslint:
        patterns:
          - "@typescript-eslint/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "npm"
    directory: "/projects/portfolio-hub"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      typescript-eslint:
        patterns:
          - "@typescript-eslint/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "npm"
    directory: "/projects/travel-packing-app"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      typescript-eslint:
        patterns:
          - "@typescript-eslint/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "npm"
    directory: "/projects/smart-recipe-app"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      typescript-eslint:
        patterns:
          - "@typescript-eslint/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "npm"
    directory: "/projects/legal-financial-rag"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      typescript-eslint:
        patterns:
          - "@typescript-eslint/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "npm"
    directory: "/projects/elder-care-planner"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      typescript-eslint:
        patterns:
          - "@typescript-eslint/*"
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
    ignore:
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
