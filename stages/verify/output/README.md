# Output (Layer 4)

This stage's output is a **CI status** (pass/fail on the PR) and, non-blocking, inline reviewdog
PR comments — neither is a repo-local file. Kept as a pointer for the same reason as the other
stage `output/` folders; see `stages/sense/output/README.md`.
