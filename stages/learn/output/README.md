# Output (Layer 4)

This stage's real output is `harness-history.json` at the **repo root** (git-tracked, unlike
`harness-status.json`) plus, when applicable, new lesson bullets in `.agents/AGENTS.md` §6. Kept
as a pointer rather than a copy for the same reason as the other stage `output/` folders; see
`stages/sense/output/README.md`.
