import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harness.legalfinancialrag',
  appName: 'legal-financial-rag',
  webDir: 'dist',
};

export default config;
