// Types are inferred from the Zod schemas in ./schemas (contract-first).
// Import from either module; they resolve to the same shapes.
export type { Garment, Outfit, DayItinerary, WearabilityReport } from './schemas';
