// GitHub Pages serves this app from this subpath. The Capacitor Android build
// (CAPACITOR_BUILD=1, see next.config.ts) uses an empty basePath instead,
// because Capacitor serves the bundle from https://localhost/ inside the
// WebView, not from the Pages subpath.
export const pagesBasePath = "/agentic-app-harness/elder-care-planner";
