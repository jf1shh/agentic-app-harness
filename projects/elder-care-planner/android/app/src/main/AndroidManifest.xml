<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!--
      No <uses-permission> block. This app makes zero network requests — see
      public/privacy.html and store-listing/README.md's Data Safety section —
      and its outbound resource links (StartingGuidePanel, LedgerReceipt) are
      external http(s) URLs that Capacitor's default WebViewClient hands off
      to the system browser via an ACTION_VIEW intent rather than loading
      in-app, so they need the browser's own INTERNET permission, not this
      app's. Requesting INTERNET here (the unmodified Capacitor scaffold
      default) would contradict the privacy policy's "no outbound network
      requests" claim in front of both Play's Data Safety reviewers and
      anyone who reads the manifest.
    -->
</manifest>
